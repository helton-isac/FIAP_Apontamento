import React from 'react'

const NotFoundPage = () => {
    return (
        <div style={style.title}>404 Not Found!</div>
    );
}

const style = {
    title: {
        color: "#FFFFFF",
        fontSize: 30,
        textAlign: "center",
        flex:1,
    },
}

export default NotFoundPage;